{
  "name": "@oah/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "pnpm --filter @oah/web build && pnpm build && electron ./dist/main.js"
  },
  "dependencies": {
    "@oah/api-contracts": "workspace:*"
  },
  "devDependencies": {
    "electron": "^39.0.0"
  }
}
